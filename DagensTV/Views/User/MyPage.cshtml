@model IEnumerable<DagensTV.Models.Channel>

@{
    ViewBag.Title = "Min Sida";
}

<h2>Min Sida</h2>

@if (Model != null)
{
    foreach (var channel in Model)
    {
        <section class="my-channeList">
            
            <ul>
                <li>
                    @Html.CheckBox("MyPage")<img src="@channel.LogoFilePath" class="channel" title="@channel.Name" alt="@channel.Name">
                </li>
            </ul>
        </section>
    }
}
